{
  "name": "posine-full",
  "version": "0.1.0",
  "author": "David Mendoza",
  "license": "GNU GPLv3",
  "description": "Sistema de ventas hecho en react con un restful api hecho en nodejs y express",
  "private": true,
  "main": "main.js",
  "productName": "POSine",
  "scripts": {
    "client": "cd Client && npm start",
    "server": "cd Server && npm start",
    "start": "concurrently -k \"npm run client\" \"npm run server\"",
    "package-mac": "node pack . --overwrite --platform=mac --icon=assets/Icons/mac-icon.icns --prune --out=builds",
    "package-win": "node pack . --overwrite --platform=win --icon=assets/Icons/win-icon.ico --prune --out=builds ",
    "package-lin": "node pack . --overwrite --platform=lin --icon=assets/Icons/lin-icon.png --prune --out=builds"
  },
  "keywords": [],
  "devDependencies": {
    "concurrently": "^3.6.1",
    "electron": "^5.0.6",
    "electron-packager": "^14.0.0",
    "yargs": "^13.2.4"
  },
  "dependencies": {
    "electron-serve": "^0.3.0"
  }
}
